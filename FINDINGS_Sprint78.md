# Findings Tracker: Sprint 78 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Finding | Status | Resolution |
|---|-------|----------|---------|--------|------------|
| 1 | R1 | High | Stale KeyStateCache design violates KERI spec and introduces a signature validation vulnerability | ADDRESSED | Redesigned: KEL sequence number for tie-breaking, freshness guard (120s) for unbounded entries, valid_until from next KEL establishment event. Rotation test added. |
| 2 | R1 | High | Sharing mutable objects by reference in verification cache creates cross-request data contamination risk | ADDRESSED | Removed deep-copy optimization entirely. Existing deep-copies retained for safety. |
| 3 | R1 | High | Plan adds public API fields and config without documentation deliverables | ADDRESSED | Added Documentation Updates section. Removed API response timing — internal logging only. |
| 4 | R1 | Medium | Shared client defaults weaken redirect controls and SSRF risk for OOBI fetches | ADDRESSED | Explicit max_redirects=3 on shared client. Per-request timeout passed to client.get(). |
| 5 | R1 | Medium | Injecting client into fetch_dossier couples it to service networking logic | ADDRESSED | Created common-package shared client instead. No API change to fetch_dossier(). |
| 6 | R1 | Medium | Revocation status caching logic underspecified | ADDRESSED | Clarified out of scope. Existing behavior unchanged. |
| 7 | R1 | Medium | Inconsistent terminology and missing documentation | ADDRESSED | Added Terminology subsection with precise definitions. |
| 8 | R1 | Low | Timing field in API response ambiguous, may leak internal data | ADDRESSED | Removed from API. Internal structured logging only. |
| 9 | R1 | Low | "No open questions" overconfident, misses immutability decision | ADDRESSED | Added Open Questions section. Immutability resolved by retaining deep-copies. |
| 10 | R2 | High | Freshness guard logic defeated by frequent access (uses last_access instead of creation time) | ADDRESSED | Changed to `cached_at` field (immutable creation time). `last_access` no longer used for freshness. |
| 11 | R2 | High | SSRF vulnerability in dossier fetch with unvalidated URLs | ADDRESSED | URL scheme validation added (http/https only). Host check added. |
| 12 | R2 | Medium | `get_for_time()` is a multi-responsibility god function | ADDRESSED | Decomposed into 3 focused methods: `_exact_match_locked`, `_range_match_locked`, `_entry_covers_time`. |
| 13 | R2 | Medium | Race condition in shared HTTP client lazy initialization | ADDRESSED | `asyncio.Lock` guards init. `get_shared_client()` is now async with double-check locking. |
| 14 | R2 | Medium | Time-index memory exhaustion from attacker-controlled timestamps | ADDRESSED | Capped at `max_time_index_entries` (10,000) with bulk eviction of oldest half. |
| 15 | R2 | Medium | Config variable not fully planned for implementation | ADDRESSED | Added to `config.py` in files table. Specified how value flows to `CacheConfig` via `kel_resolver.py:get_cache()`. |
| 16 | R2 | Medium | HTTP client duplicated across two modules | ADDRESSED | Single canonical implementation in `common/common/vvp/http_client.py`. Verifier re-exports. |
| 17 | R2 | Low | Missing CHANGES.md update | ADDRESSED | Added to Documentation Updates section. |
| 18 | R3 | High | SSRF mitigation is incomplete; it blocks invalid schemes but still allows requests to private and loopback IP address... | ADDRESSED | Added `_validate_url_target()` — resolves hostname to IP, blocks private/loopback/link-local/reserved ranges. Applied to `fetch_dossier()` and `dereference_oobi()`. Redirect targets validated via httpx event_hooks. |
| 19 | R3 | High | The plan documents that `get_shared_client()` is now `async`, but code snippets in multiple locations call it without... | ADDRESSED | Fixed `fetch_dossier()` snippet to `client = await get_shared_client()`. All code snippets now use `await`. |
| 20 | R3 | High | The "Freshness Guard" documentation text still describes the stale detection logic as being based on "last accessed/p... | ADDRESSED | Freshness Guard section now states: "entry's immutable creation timestamp (`cached_at`)" — removed all "last accessed/populated" language. |
| 21 | R3 | Medium | `KeyStateCache` is accumulating too many responsibilities (storage, lookup, KEL semantics, eviction policy), which in... | ADDRESSED | Added Responsibility Separation section: storage/eviction (KeyStateCache), window matching (standalone `_entry_covers_time()`), lookup orchestration (`get_for_time()`). Each independently testable. |
| 22 | R3 | Medium | The plan has conflicting guidance on the ownership of HTTP client configuration between the component narrative and t... | ADDRESSED | Files table updated: common owns canonical impl, verifier re-exports (3 lines). Path/import convention section clarifies filesystem vs Python import naming. |
| 23 | R3 | Medium | The time-index eviction mechanism can be triggered by an attacker flooding the system with unique timestamps, creatin... | ADDRESSED | Eviction enforced BEFORE insert (`>=` check, not `>` after). Documented as security parameter scaled to `max_entries`. |
| 24 | R3 | Medium | The test utility `reset_shared_client()` is a synchronous function that does not properly close the async client, cre... | ADDRESSED | `reset_shared_client()` now async, calls `aclose()` on open client, guarded by `assert "pytest" in sys.modules`. |
| 25 | R3 | Medium | The OOBI API includes a `max_redirects` parameter that is documented as non-authoritative, creating a misleading and ... | ADDRESSED | Removed `max_redirects` from `dereference_oobi()` signature. Controlled at client level only. |
| 26 | R3 | Medium | Path and module terminology (e.g., `common/common/vvp` vs. `common.vvp`) is used inconsistently throughout the plan, ... | ADDRESSED | Added path/import convention note: filesystem paths use `common/common/vvp/...`, Python imports use `from common.vvp...`. Dual naming inherent to monorepo. |
| 27 | R3 | Low | The performance test proposal couples tests to brittle latency ratios (e.g., "second call ≤50% of first"), which are ... | ADDRESSED | Changed to absolute thresholds: Phase 4 < 5ms, total < 100ms. No relative ratio assertions. |
| 28 | R3 | Low | The configurable freshness guard window (`VVP_KEY_STATE_FRESHNESS_WINDOW_SECONDS`) lacks documented minimum or maximu... | ADDRESSED | Added bounds: min 10s, max 3600s. Values outside range clamped with startup warning. |
| 29 | R4 | High | Stale-key acceptance window permits authentication bypass after key rotation (File: `PLAN_Sprint78.md`, Location: Com... | ADDRESSED | Documented as explicit trade-off with mitigations: operator-configurable window (min 10s), Phase 9 revocation independent, PASSporT expiry limits forged token lifetime. Added to Risks table. |
| 30 | R4 | High | SSRF mitigation is vulnerable to DNS rebinding (TOCTOU) between validation and connection (File: `PLAN_Sprint78.md`, ... | ADDRESSED | `follow_redirects=False` eliminates redirect-based SSRF. DNS rebinding TOCTOU between `validate_url_target()` and httpx connect is an accepted trade-off — documented in url_validation.py module docstring with mitigations (see finding #65). |
| 31 | R4 | High | SSRF protection is bypassed on redirects because the described `event_hooks` are not implemented in the client creati... | ADDRESSED | Removed event_hooks approach entirely. `follow_redirects=False` is simpler and eliminates redirect-based bypass. 3xx responses treated as errors. |
| 32 | R4 | Medium | Synchronous, blocking DNS call (`socket.getaddrinfo`) in an async code path stalls the event loop, undermining perfor... | ADDRESSED | Replaced with `loop.getaddrinfo()` — async DNS resolution via event loop. No blocking I/O. |
| 33 | R4 | Medium | Shared HTTP module has too many responsibilities, mixing client lifecycle, URL validation, and test utilities, which ... | ADDRESSED | Split into two modules: `http_client.py` (client lifecycle only) + `url_validation.py` (SSRF validation only). |
| 34 | R4 | Medium | Private-style naming (`_validate_url_target`) is used for a public, cross-module API, creating an unclear maintenance... | ADDRESSED | Renamed to `validate_url_target()` (public API naming). Lives in `common.vvp.url_validation`. |
| 35 | R4 | Medium | Documentation and code are inconsistent on whether window matching logic is a pure function or an instance method, in... | ADDRESSED | `_entry_covers_time()` consistently shown as module-level pure function with explicit `freshness_window_seconds` parameter. Callers pass `self._config.freshness_window_seconds`. |
| 36 | R4 | Medium | URL validation logic is duplicated in both the high-level fetcher and the low-level HTTP client (File: `common/common... | ADDRESSED | Single validation point: callers invoke `validate_url_target()` once. No inline scheme/host checks in `fetch_dossier()`. No validation in HTTP client module. |
| 37 | R4 | Medium | Plaintext `http://` is permitted for fetching untrusted, attacker-influenced URLs, enabling traffic interception (Fil... | ADDRESSED | Dossier URLs require `https` only (default `allow_http=False`). OOBI URLs allow `http` via explicit `allow_http=True` (local witnesses). |
| 38 | R1 | High | Potential for unbounded memory growth in `KeyStateCache` due to an unpruned `_time_index`, leading to memory exhausti... | ADDRESSED | Extracted `_enforce_time_index_cap()` method. Called in both `put()` and `_range_match_locked()`. Cap enforced BEFORE inserting new entries. |
| 39 | R1 | High | The critical correctness guarantee of the range-based cache (`valid_until` population) is not tested, meaning a regre... | ADDRESSED | Added 3 tests: `test_valid_until_none_when_no_subsequent_rotation`, `test_valid_until_set_to_next_rotation_time`, `test_valid_until_correct_with_multiple_rotations`. |
| 40 | R1 | High | A required end-to-end regression test for cache behavior during a key rotation event is completely missing, failing t... | ADDRESSED | Added `TestCacheKeyRotation` class with 2 tests: `test_rotation_splits_cache_correctly` (pre/post/boundary queries) and `test_before_inception_returns_none`. |
| 41 | R1 | High | The concurrency test for the shared HTTP client does not actually test for concurrency, providing a false sense of se... | ADDRESSED | Rewrote `test_concurrent_get_safe` to patch `httpx.AsyncClient.__init__` with a counting wrapper. Asserts `creation_count == 1` — proves the lock prevented duplicate creation. |
| 42 | R1 | High | The test for cache range matching is flawed and only tests the exact-match path, leaving the primary range-scan logic... | ADDRESSED | Both `test_get_for_time_range_match` (test_kel_cache.py) and new `test_range_scan_verified_not_exact_match` (test_key_state_cache_range.py) now assert `(aid, rt) not in cache._time_index` before query and `in` after, proving range scan path is exercised. |
| 43 | R1 | High | The project change log has not been updated for Sprint 78, making it impossible for operators to track the substantia... | ADDRESSED | Added Sprint 78 section to CHANGES.md with summary, key changes, and files changed table. |
| 44 | R1 | High | Critical knowledge documentation for the new range-based cache semantics and the `VVP_KEY_STATE_FRESHNESS_WINDOW_SECO... | ADDRESSED | Updated `knowledge/verification-pipeline.md` Phase 4 with range-based cache details. Added `VVP_KEY_STATE_FRESHNESS_WINDOW_SECONDS` to `knowledge/deployment.md` environment variables table. |
| 45 | R1 | Medium | The dossier fetch module contains inconsistent error handling for redirects, dead code branches, and outdated documen... | ADDRESSED | Removed dead `DOSSIER_MAX_REDIRECTS` import from fetch.py. Module docstring already reflects Sprint 78 changes (no redirects, shared client). |
| 46 | R1 | Medium | Generic URL validation logic is tightly coupled to dossier-specific error types, violating module boundaries and hind... | ADDRESSED | Created domain-agnostic `URLValidationError(Exception)` in url_validation.py. Callers catch and re-raise as their domain exception (FetchError in fetch.py, ResolutionFailedError in oobi.py). |
| 47 | R1 | Medium | Configuration logic for the freshness window is duplicated between the config module and the KEL resolver, creating t... | ADDRESSED | `kel_resolver.py:get_cache()` now imports `VVP_KEY_STATE_FRESHNESS_WINDOW_SECONDS` from `app.core.config` instead of re-reading env. Single source of truth. |
| 48 | R1 | Medium | Critical SSRF test cases for cloud metadata (`169.254.169.254`) and IPv6 loopback (`::1`) endpoints, specified in the... | ADDRESSED | Added `test_cloud_metadata_rejected` (169.254.169.254), `test_loopback_ipv6_rejected` (::1), `test_link_local_rejected` (169.254.1.1). |
| 49 | R1 | Medium | Tests directly mutate private global state (`_shared_client`) instead of using the public reset API, violating encaps... | ADDRESSED | Added `reset_shared_client_sync()` public API to http_client.py. conftest.py now uses it instead of `_http_client_mod._shared_client = None`. |
| 50 | R1 | Medium | Module and function docstrings for the KERI cache and other modules are outdated and do not reflect the new range-bas... | ADDRESSED | Updated module docstring and KeyStateCache class docstring to describe range-based matching, freshness guard, time-index cap, and validity windows. |
| 51 | R1 | Low | Repeated test setup logic for mocking HTTP clients and URL validation is duplicated across multiple test files instea... | ADDRESSED | Added `_mock_dns()` helper in test_connection_pooling.py. Other files use contextual helpers (`mock_oobi_http`, `_mock_fetch_dossier_context`) appropriate to their domain. Full centralization would add indirection without proportional benefit. |
| 52 | R2 | High | The critical key rotation regression test cannot be verified as existing, as the file is truncated; this is the most ... | ADDRESSED | File is complete — `TestCacheKeyRotation` has `test_rotation_splits_cache_correctly` and `test_before_inception_returns_none`. Reviewer saw truncated context, not truncated file. |
| 53 | R2 | High | The test for the time-index eviction cap is missing; this cap is a critical defense against memory exhaustion attacks... | ADDRESSED | Rewrote `TestTimeIndexEviction` with 3 tests: `test_time_index_enforced_at_cap` (size <= cap), `test_eviction_removes_oldest_half`, `test_cache_functional_after_eviction`. |
| 54 | R2 | High | The concurrency test for the shared HTTP client's double-checked locking pattern is flawed and does not create a real... | ADDRESSED | Rewrote `test_concurrent_get_safe` using `asyncio.Event` gate — all 20 coroutines wait, then release simultaneously to create genuine contention for the asyncio.Lock. |
| 55 | R2 | High | The new, critical shared client module (`http_client.py`) lacks docstrings for its public API, making its concurrency... | ADDRESSED | Enhanced docstrings for `close_shared_client()`, `reset_shared_client()`, and `reset_shared_client_sync()` — lifecycle, safety guards, and usage context documented. |
| 56 | R2 | High | The safety guard in `reset_shared_client_sync()` that prevents it from being run outside of a test environment is unt... | ADDRESSED | Added `test_reset_sync_safety_guard` (hides `pytest` from `sys.modules`, asserts `AssertionError`) and `test_reset_sync_clears_client` (verifies singleton reset). |
| 57 | R2 | Medium | The security-relevant clamping behavior for the `freshness_window_seconds` configuration is untested, meaning an oper... | ADDRESSED | Added `TestFreshnessWindowClamping` with 4 tests: normal value, below-minimum clamped to 10, above-maximum clamped to 3600, clamping logs warning. |
| 58 | R2 | Medium | An older cache test uses naive datetimes while the cache normalizes to UTC, creating a fragile test that could pass f... | ADDRESSED | Updated all `TestCacheTimeIndex` datetimes to use `timezone.utc`. Added `timezone` import. |
| 59 | R2 | Medium | A test re-imports a module (`fetch_dossier`) inside the test body, which can bypass mocks if the module was already c... | ADDRESSED | Moved `from common.vvp.dossier.fetch import fetch_dossier` to top of test method, before `with _mock_dns(...)` block. |
| 60 | R2 | Medium | URL validation logic is duplicated between a high-level function (`dereference_oobi`) and a low-level utility (`valid... | ADDRESSED | Removed inline `urlparse` scheme/netloc check from `dereference_oobi()`. Single validation point: `validate_url_target()` checks scheme, host, and DNS/IP. |
| 61 | R2 | Medium | A dead configuration variable (`DOSSIER_MAX_REDIRECTS`) remains in the code and admin API even though the client's re... | ADDRESSED | Removed `DOSSIER_MAX_REDIRECTS` from `config.py`, `common/dossier/config.py`, `__init__.py`, verifier shim, and admin endpoint. Admin shows `follow_redirects: false`. |
| 62 | R2 | Low | Inconsistent phrasing is used for HTTP status error messages. (File: `common/common/vvp/dossier/fetch.py`, Location: ... | ADDRESSED | Standardized both HTTP error messages to `"HTTP {code} fetching dossier..."` format. |
| 63 | R2 | Low | The test reset API for the shared client is split into two overlapping functions (`reset_shared_client` and `reset_sh... | ADDRESSED | Both are intentional: async version calls `aclose()` (for async tests), sync version skips `aclose()` (for sync fixtures). Docstrings clarify the distinction. |
| 64 | R2 | Low | Test scaffolding, such as mock data classes and helper functions for the cache, is duplicated across multiple test fi... | ADDRESSED | Each test file uses contextual helpers appropriate to its domain. Centralizing would add indirection without proportional benefit (same resolution as R1 finding #51). |
| 65 | R3 | High | SSRF mitigation is vulnerable to DNS rebinding (TOCTOU) because the IP address is validated at check-time but the hos... | ADDRESSED | Documented as accepted trade-off in url_validation.py module docstring. Mitigations: TOCTOU window is milliseconds, `follow_redirects=False`, URLs from VVP-Identity `kid` (attacker already controls OOBI URL), cloud metadata blocked by IP range regardless. Full DNS pinning deferred (requires custom httpx transport). |
| 66 | R4 | High | Unauthenticated SSRF primitive in `/proxy-fetch` bypasses all new SSRF hardening (File: `services/verifier/app/main.p... | ADDRESSED | `_fetch_dossier_logic()` now uses `validate_url_target()` + `get_shared_client()` with SSRF checks. No standalone `httpx.AsyncClient`. |
| 67 | R4 | High | DNS rebinding (TOCTOU) vulnerability remains exploitable between URL validation and connection, and was incorrectly m... | ADDRESSED | Corrected finding #30 resolution wording. TOCTOU already documented as accepted trade-off in url_validation.py module docstring (see finding #65). Mitigations: millisecond window, `follow_redirects=False`, attacker already controls OOBI URL, cloud metadata blocked by IP range. |
| 68 | R4 | High | Missing critical end-to-end regression test to verify the main sprint objective: that a second verification call with... | ADDRESSED | Added `TestCachedSecondCallRegression.test_second_call_uses_cache_no_refetch` in test_connection_pooling.py. Verifies `oobi_fetch_count` stays at 1 after second `resolve_key_state()` call with different `iat`. |
| 69 | R4 | Medium | Plain-HTTP is permitted for OOBI resolution against any host, creating a transport downgrade risk for key-state retri... | ADDRESSED | By design: local witnesses use HTTP (docker-compose, staging). `allow_http=True` is explicit and intentional for OOBI. Dossier fetch enforces `allow_http=False` (https only). Policy difference now asserted by `TestUrlValidationPolicy` tests. |
| 70 | R4 | Medium | Split source-of-truth for dossier fetch policy (timeout/size) increases coupling and risk of configuration drift betw... | ADDRESSED | Removed duplicate `DOSSIER_FETCH_TIMEOUT_SECONDS` and `DOSSIER_MAX_SIZE_BYTES` definitions from verifier config.py. Now re-exported via `from common.vvp.dossier.config import ...`. Single source of truth in common. |
| 71 | R4 | Medium | Backward-compatibility shims create unnecessary indirection and inconsistent import patterns, blurring ownership of c... | ADDRESSED | Removed `app/vvp/http_client.py` shim entirely. All 3 consumers (main.py, oobi.py, tel_client.py) now import directly from `common.vvp.http_client`. Dossier fetch shim simplified — removed config re-exports. |
| 72 | R4 | Medium | Tests for TTL expiry are timing-dependent (using `asyncio.sleep`) and can be flaky under load (File: `services/verifi... | ADDRESSED | Replaced `asyncio.sleep(1.5)` with direct manipulation of `entry.expires_at` to a past timestamp. No sleep, no timing dependency. |
| 73 | R4 | Medium | URL validation policy is not asserted in tests, allowing security regressions (e.g., `allow_http` flag) to go undetec... | ADDRESSED | Added `TestUrlValidationPolicy` with 2 tests: `test_dossier_fetch_rejects_http` (asserts `allow_http=False`) and `test_oobi_allows_http` (asserts `allow_http=True`). Uses spy function to capture kwargs. |
| 74 | R4 | Medium | Module documentation is outdated and no longer matches current implementation (e.g., council reviewer count, type ann... | ADDRESSED | Module docstrings already updated in R2 (cache.py, http_client.py). Council config is in `scripts/council-config.json`, not in docstrings. Type annotations are runtime Python, not documentation. |
| 75 | R4 | Low | Potential for unbounded growth in `_time_index` if `max_time_index_entries` is not carefully configured (File: `servi... | ADDRESSED | Already addressed in R2 finding #14: capped at `max_time_index_entries` (default 10,000) with bulk eviction. `_enforce_time_index_cap()` called in both `put()` and `_range_match_locked()`. |
| 76 | R4 | Low | Redundant error-handling path in `fetch_dossier()` adds cognitive overhead (File: `common/common/vvp/dossier/fetch.py... | ADDRESSED | Removed `except FetchError: raise` — FetchError is not a subclass of any httpx exception, so the specific handlers (`httpx.TimeoutException`, `httpx.HTTPStatusError`, `httpx.RequestError`) never catch it. |
| 77 | R4 | Low | Overlapping test coverage for range-cache behavior increases maintenance cost (File: `services/verifier/tests/test_ke... | ADDRESSED | Same resolution as R1 #51 and R2 #64: test files cover different concerns (test_kel_cache.py = basic cache ops, test_key_state_cache_range.py = range/rotation/eviction). Overlap provides regression resilience. |
| 78 | R5 | High | The performance benefit of the new cache is not realized on the delegation resolution path because caching is omitted... | ADDRESSED | Added `cache.put()` call to `resolve_key_state_with_kel()` after finding the key state. The delegation path still fetches OOBI (needs full KEL events), but now caches the resulting KeyState for future lookups by either path. |
| 79 | R6 | High | DNS rebinding/TOCTOU SSRF vulnerability remains in the URL validation path, and the risk is not acceptable for produc... | WONTFIX | Known Debt. Duplicate of #30, #65, #67. Accepted trade-off documented in url_validation.py module docstring. Full DNS pinning requires custom httpx transport — deferred to future security sprint. |
| 80 | R6 | High | Missing critical integration test to verify that a cache hit in `resolve_key_state()` avoids a redundant network fetc... | ADDRESSED | Already added in R4: `TestCachedSecondCallRegression.test_second_call_uses_cache_no_refetch` in test_connection_pooling.py. Reviewer may have expected it in test_kel_resolver.py; it lives in test_connection_pooling.py alongside other integration tests. |
| 81 | R6 | High | The `dossier` configuration module has become a cross-domain "god config" with outdated documentation, mixing unrelat... | WONTFIX | Known Debt. Pre-existing architecture not in Sprint 78 scope. Config consolidation (#70) reduced duplication; full restructure deferred to dedicated config refactor sprint. |
| 82 | R6 | High | The council review script's module docstring is severely outdated, misrepresenting the number of reviewers and config... | ADDRESSED | Updated docstring: "up to 8 focused expert reviewers across 3 AI platforms". |
| 83 | R6 | Medium | Plaintext HTTP is broadly permitted for OOBI fetches without sufficient restriction, creating a transport security ri... | WONTFIX | Known Debt. Duplicate of #69. By design: local witnesses use HTTP. `allow_http=True` is explicit and intentional. Policy asserted by `TestUrlValidationPolicy`. |
| 84 | R6 | Medium | Egress traffic to untrusted URLs is not controlled by a configurable domain/port allowlist, allowing the service to c... | WONTFIX | Known Debt. New feature request, not Sprint 78 scope. Egress allowlist is a network-level concern (Azure NSG/firewall). |
| 85 | R6 | Medium | Configuration logic is duplicated, tightly coupled to implementation, and its parameters have an unclear relationship... | WONTFIX | Known Debt. Duplicate of #70. Config consolidated to single source of truth in common. CacheConfig docstring explains all parameters. Full config refactor deferred. |
| 86 | R6 | Medium | Regression test coverage for `3xx` redirect handling is incomplete; the new behavior of treating redirects as errors ... | ADDRESSED | Added `TestRedirectHandling` with 2 tests: `test_shared_client_returns_redirect_as_is` (asserts `follow_redirects=False`) and `test_dossier_fetch_rejects_redirect` (asserts 302 raises FetchError). |
| 87 | R6 | Medium | Documentation contains drift and defects, including caller-specific notes in a shared module and an incorrect public ... | ADDRESSED | Removed Sprint 78 caller-specific note from url_validation.py. Fixed `Dict[str, any]` → `Dict[str, Any]` type annotation in cache.py. |
| 88 | R6 | Medium | A concurrency test contains an un-awaited coroutine, which can cause runtime warnings and potential test flakiness. (... | ADDRESSED | Removed dead `gate.wait()` line (un-awaited coroutine) from `test_concurrent_get_safe`. |
| 89 | R6 | Low | A dead/duplicated error handling path exists in the dossier fetch logic. (File: `common/common/vvp/dossier/fetch.py`,... | ADDRESSED | Already removed in R4 finding #76. Duplicate finding — `except FetchError: raise` was deleted in commit 655351d. |
| 90 | R6 | Low | Potential for increased memory usage due to `_time_index` growth (Source: Performance Expert) — Reason: This is a dup... | WONTFIX | Known Debt. Duplicate of #14, #38, #75. Already capped at max_time_index_entries with bulk eviction. Excluded by consolidator. |
| 91 | R6 | Low | Missing focused tests | WONTFIX | Known Debt. Vague finding with no specific test gap identified. Existing coverage is comprehensive (1900+ tests). |
